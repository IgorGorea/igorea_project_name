<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">
    <Properties>
        <Property name="logDir">${ctx:screensPath}</Property>
    </Properties>
    <Appenders>
        <RollingFile name="AllLogsFileAppender" fileName="${logDir}/all/all_logs-${ctx:scenarioName}-${date:MM-dd-yyyy-HH-mm-ss}.log"
                     filePattern="${logDir}/all/all_logs-%d{MM-dd-yyyy}-%i.log.gz">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
            <Policies>
                <SizeBasedTriggeringPolicy size="3 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/> <!-- Maximum number of files to keep -->
            <Delete basePath="${logDir}" maxDepth="3">
            <IfFileName glob="all_logs-*.log" />
            <IfLastModified age="7d" />
            </Delete>
        </RollingFile>

        <RollingFile name="InfoLogsFileAppender" fileName="${logDir}/info/info_logs-${ctx:scenarioName}-${date:MM-dd-yyyy-HH-mm-ss}.log"
                     filePattern="${logDir}/info/info_logs-%d{MM-dd-yyyy}-%i.log.gz">
            <ThresholdFilter level="INFO" onMatch="ACCEPT" onMismatch="DENY"/>
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
            <Policies>
                <SizeBasedTriggeringPolicy size="3 MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/> <!-- Maximum number of files to keep -->
            <Delete basePath="${logDir}" maxDepth="3">
                <IfFileName glob="info_logs-*.log" />
                <IfLastModified age="7d" />
            </Delete>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n"/>
        </Console>
    </Appenders>

    <Loggers>
        <Root level="debug">
            <AppenderRef ref="AllLogsFileAppender"/>
            <AppenderRef ref="InfoLogsFileAppender"/>
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>
